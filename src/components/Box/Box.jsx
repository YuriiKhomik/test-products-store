import styled from "styled-components";
import { space, layout, color, flexbox, border, position } from "styled-system";

export const Box = styled("div")(
  space,
  layout,
  color,
  flexbox,
  border,
  position
);
